/// <reference types="vite/client" />

/** Baked in by `vite.config.ts`'s `define` from `computeAppVersion()`, so the
 *  About dialog can show which build it is without anything to keep in sync. */
declare const __APP_VERSION__: string
